import * as React from "react";
import Autocomplete from "@mui/joy/Autocomplete";

export const SearchGroup = () => {
  return (
    <Autocomplete placeholder="Combo box" options={[]} sx={{ width: 300 }} />
  );
};
